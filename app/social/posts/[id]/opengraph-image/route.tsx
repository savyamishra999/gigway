import { ImageResponse } from "next/og";import { resolvePostAccess,safePost } from "@/lib/social/server";export const runtime="nodejs";export async function GET(_r:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params,post=await resolvePostAccess(id);if(!post||post.visibility!=="public")return new Response("Not found",{status:404});const value=await safePost(post),author=value.author?.name||"GigWay member",username=value.author?.username?`@${value.author.username}`:"",text=(post.body||"View this post on GigWay.").replace(/<[^>]*>/g,"").replace(/\s+/g," ").trim().slice(0,220);return new ImageResponse(<div style={{display:"flex",width:"100%",height:"100%",padding:64,background:"#f8fafc",fontFamily:"sans-serif",flexDirection:"column",justifyContent:"space-between",color:"#111827"}}><div style={{display:"flex",color:"#4f46e5",fontSize:34,fontWeight:800}}>GigWay</div><div style={{display:"flex",flexDirection:"column"}}><div style={{display:"flex",fontSize:30,fontWeight:700}}>{author}</div><div style={{display:"flex",fontSize:20,color:"#64748b",marginTop:8}}>{username}</div><div style={{display:"flex",fontSize:38,lineHeight:1.25,marginTop:32}}>{text}</div></div><div style={{display:"flex",fontSize:18,color:"#4f46e5"}}>gigway.in · Professional Network & Marketplace</div></div>,{width:1200,height:630})}
