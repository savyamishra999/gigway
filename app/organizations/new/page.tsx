import { redirect } from "next/navigation"
import { createClient } from "@/lib/supabase/server"
import OrganizationForm from "@/components/organizations/OrganizationForm"
export default async function NewOrganizationPage() { const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login"); return <main className="min-h-screen bg-[#0A0A0F] px-4 py-10 pb-24"><section className="mx-auto max-w-2xl"><div className="mb-8"><p className="text-xs font-bold tracking-[.16em] text-[#A99FFF]">ORGANIZATION IDENTITY</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-white">Build your company on GigWay.</h1><p className="mt-3 max-w-xl text-[#98A1B3]">Your personal profile stays separate. Create a professional home for your team, opportunities and company presence.</p></div><div className="rounded-3xl bg-[#15151d] p-5 sm:p-8 shadow-2xl shadow-black/20 ring-1 ring-white/8"><OrganizationForm /></div></section></main> }
