import { notFound, redirect } from "next/navigation"
import { createClient } from "@/lib/supabase/server"
import OrganizationForm from "@/components/organizations/OrganizationForm"
export default async function EditOrganization({ params }: { params: Promise<{ username: string }> }) { const { username } = await params; const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login"); const { data: org } = await supabase.from("organizations").select("*").eq("username", username).maybeSingle(); if (!org) notFound(); const { data: membership } = await supabase.from("organization_members").select("member_role").eq("organization_id", org.id).eq("profile_id", user.id).eq("status", "active").maybeSingle(); if (!membership || !["owner", "admin"].includes(membership.member_role)) redirect(`/@${username}`); return <main className="min-h-screen bg-[#0A0A0F] py-10 px-4"><section className="max-w-2xl mx-auto bg-[#12121A] border border-[#1E1E2E] rounded-2xl p-6"><h1 className="text-2xl font-bold text-white mb-6">Edit Organization</h1><OrganizationForm organization={org} /></section></main> }
