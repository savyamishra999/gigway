import Link from "next/link"
import { ArrowRight, CheckCircle2, Search } from "lucide-react"
import { createClient } from "@/lib/supabase/server"
import SocialHomeFeed from "@/components/social/SocialHomeFeed"

export default async function HomeHub(){
  const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser()
  if(!user)return <main className="min-h-screen bg-brand-ivory px-4 py-20 text-center"><h1 className="text-h1 font-extrabold text-brand-midnight">Your next opportunity starts here.</h1><Link href="/login" className="mt-6 inline-block rounded-xl bg-brand-indigo px-5 py-3 font-bold text-white">Join GigWay</Link></main>
  const[{data:profile},{data:jobs},{data:projects},{data:people},{data:organizations},{count:unread},{count:applications},{count:proposals}]=await Promise.all([
    supabase.from("profiles").select("full_name,skills").eq("id",user.id).maybeSingle(),
    supabase.from("jobs").select("id,title,company_name,location,created_at,organizations:organization_id(name,username,logo_url)").eq("status","active").order("created_at",{ascending:false}).limit(6),
    supabase.from("projects").select("id,title,budget,category,created_at").eq("status","open").order("created_at",{ascending:false}).limit(6),
    supabase.from("profiles").select("id,full_name,username,avatar_url,tagline,skills,is_verified").eq("profile_completed",true).neq("id",user.id).order("created_at",{ascending:false}).limit(6),
    supabase.from("organizations").select("id,name,username,logo_url,tagline,industry").order("created_at",{ascending:false}).limit(6),
    supabase.from("messages").select("id",{count:"exact",head:true}).eq("receiver_id",user.id).eq("is_read",false),
    supabase.from("job_applications").select("id",{count:"exact",head:true}).eq("applicant_id",user.id),
    supabase.from("proposals").select("id",{count:"exact",head:true}).eq("freelancer_id",user.id),
  ])
  const first=profile?.full_name?.split(" ")[0]||"there"
  return <main className="min-h-screen max-w-full overflow-x-clip bg-brand-ivory pb-24 lg:pb-16"><div className="mx-auto max-w-7xl px-4 py-8 sm:py-10"><header className="mx-auto max-w-3xl"><p className="text-caption font-bold tracking-[.16em] text-brand-coral">GIGWAY NETWORK</p><h1 className="mt-2 text-h2 font-extrabold text-brand-midnight sm:text-h1">Welcome back, {first}.</h1><p className="mt-2 text-body-sm text-brand-slate sm:text-body-lg">Professional conversations and opportunities, in one place.</p><form action="/explore" className="relative mt-6 w-full"><Search className="absolute left-4 top-1/2 h-5 w-5 -translate-y-1/2 text-brand-indigo"/><input name="q" placeholder="Search people, jobs, projects, services..." className="w-full min-w-0 rounded-pill border-2 border-brand-indigo/20 bg-white py-3.5 pl-12 pr-24 text-body-sm shadow-elevated outline-none focus:border-brand-indigo"/><button className="absolute right-2 top-2 shrink-0 rounded-pill bg-brand-indigo px-4 py-1.5 text-body-sm font-bold text-white">Search</button></form></header><div className="mx-auto grid min-w-0 max-w-5xl gap-8 lg:grid-cols-[minmax(0,1fr)_260px]"><SocialHomeFeed jobs={(jobs||[]).map(j=>({id:j.id,title:j.title,subtitle:[j.company_name,j.location].filter(Boolean).join(" · "),href:`/jobs/${j.id}`}))} projects={(projects||[]).map(p=>({id:p.id,title:p.title,subtitle:p.category||"Open project",href:`/projects/${p.id}`}))} people={(people||[]).map(p=>({id:p.id,name:p.full_name||"Professional",subtitle:p.tagline||p.skills?.slice(0,2).join(" · "),href:`/u/${p.username}`,image:p.avatar_url}))} organizations={(organizations||[]).map(o=>({id:o.id,name:o.name,subtitle:o.tagline||o.industry||"Organization",href:`/u/${o.username}`,image:o.logo_url}))}/><aside className="mt-8 space-y-4 lg:mt-8"><section className="rounded-2xl border border-brand-borderLight bg-white p-4 shadow-soft"><h2 className="font-extrabold text-brand-midnight">Your activity</h2><div className="mt-3 grid grid-cols-3 gap-2 text-center"><Link href="/messages" className="rounded-xl bg-brand-ivory p-2"><b className="block text-lg text-brand-midnight">{unread||0}</b><span className="text-[10px] text-brand-slate">Unread</span></Link><Link href="/profile" className="rounded-xl bg-brand-ivory p-2"><b className="block text-lg text-brand-midnight">{applications||0}</b><span className="text-[10px] text-brand-slate">Applied</span></Link><Link href="/profile" className="rounded-xl bg-brand-ivory p-2"><b className="block text-lg text-brand-midnight">{proposals||0}</b><span className="text-[10px] text-brand-slate">Proposals</span></Link></div></section><section className="rounded-2xl border border-brand-indigo/20 bg-brand-indigo/[.04] p-4"><CheckCircle2 className="h-5 w-5 text-brand-indigo"/><h2 className="mt-2 font-extrabold text-brand-midnight">Build your professional edge.</h2><p className="mt-1 text-caption text-brand-slate">Use GigWay&apos;s tools to sharpen your profile and applications.</p><Link href="/ai-tools" className="mt-3 inline-flex items-center gap-1 text-caption font-bold text-brand-indigo">Explore tools <ArrowRight className="h-3.5 w-3.5"/></Link></section></aside></div></div></main>
}
