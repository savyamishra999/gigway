import{NextRequest,NextResponse}from"next/server";import{canManagePost,requireSocialUser,socialDb}from"@/lib/social/server";
export async function POST(_req:NextRequest,{params}:{params:Promise<{id:string}>}){const u=await requireSocialUser();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});const{id}=await params;const db=socialDb();const{data:post}=await db.from("posts").select("id,author_user_id,author_organization_id,body").eq("id",id).maybeSingle();if(!post)return NextResponse.json({error:"Post not found."},{status:404});if(!await canManagePost(post,u.id))return NextResponse.json({error:"You cannot publish this post."},{status:403});const{count}=await db.from("post_media").select("id",{count:"exact",head:true}).eq("post_id",id);if(!post.body&&!count)return NextResponse.json({error:"Add text or media before publishing."},{status:400});const{error}=await db.from("posts").update({status:"published",updated_at:new Date().toISOString()}).eq("id",id);if(error)return NextResponse.json({error:"Could not publish post."},{status:503});return NextResponse.json({success:true})}
