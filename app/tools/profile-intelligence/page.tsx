import {redirect}from"next/navigation";import ProfileIntelligence from"@/components/ai/ProfileIntelligence";import{createClient}from"@/lib/supabase/server";export default async function Page(){const db=await createClient();const{data:{user}}=await db.auth.getUser();if(!user)redirect("/login?next=/tools/profile-intelligence");return <main className="min-h-screen bg-brand-ivory px-4 py-10"><div className="mx-auto max-w-3xl"><h1 className="text-h1 font-extrabold text-brand-midnight">Profile Intelligence</h1><p className="mt-2 text-brand-slate">See how strong your professional identity is — and what to improve next.</p><div className="mt-6"><ProfileIntelligence/></div></div></main>}
