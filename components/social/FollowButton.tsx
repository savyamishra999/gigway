"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
export type FollowTargetType = "profile" | "organization";
const EVENT = "gigway:follow-change";
export default function FollowButton({targetType,targetId,initialFollowing,label,className=""}:{targetType:FollowTargetType;targetId:string;initialFollowing:boolean;label:string;className?:string}){const router=useRouter(),[following,setFollowing]=useState(initialFollowing),[busy,setBusy]=useState(false);useEffect(()=>{const sync=(e:Event)=>{const d=(e as CustomEvent<{type:FollowTargetType;id:string;following:boolean}>).detail;if(d.type===targetType&&d.id===targetId)setFollowing(d.following)};window.addEventListener(EVENT,sync);return()=>window.removeEventListener(EVENT,sync)},[targetType,targetId]);const toggle=async()=>{if(busy)return;const before=following;setBusy(true);setFollowing(!before);try{const r=await fetch(`/api/social/follow/${targetType}/${targetId}`,{method:before?"DELETE":"POST"});if(!r.ok)throw Error();window.dispatchEvent(new CustomEvent(EVENT,{detail:{type:targetType,id:targetId,following:!before}}));router.refresh()}catch{setFollowing(before)}finally{setBusy(false)}};return <button type="button" disabled={busy} onClick={toggle} aria-label={`${following?"Unfollow":"Follow"} ${label}`} className={className}>{busy?"Working…":following?"Following":"Follow"}</button>}
